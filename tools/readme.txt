This folder contains the external tools bundled with GoLismero.

Whenever a plugin wants to invoke an external tool, this is the first location to be checked. If not found here, the PATH environment variable is used.
